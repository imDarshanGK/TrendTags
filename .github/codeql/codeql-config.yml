name: "TrendTags Analysis"
disable-default-queries: false

queries:
  - uses: codeql/python-queries@latest # Python security + quality
  - uses: codeql/javascript-queries@latest # JavaScript security + quality
